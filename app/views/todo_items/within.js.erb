var todoItems = $("#show-todo-items");
todoItems.html("");
<%# centre_point = [newCentre.lat(), newCentre.lng()].map(&:to_f) %>
<%# distance = (newDistance.to_f)/1000 %>
//$("#show-todo-items").data("todo-item-id").each(function(i) { @todoItems << i; });
<% @todo_items.each do |todo_item| %>
  <%# if todo_item.lat.present? && todo_item.lng.present? && todo_item.distance_to(centre_point) <= distance %>
  todoItems.append("<%= j(render partial: "todo_items/todo_item_map", 
			    locals: { todo_item: todo_item }) %>");
  <%# end %>
<% end %>
