$(document).ready(function(){
/*  

  $("body").on("click", "#add-destination-button", function(event){
    event.preventDefault();
    var formData = $(event.currentTarget).closest("form").serialize();
    $.ajax({
      type: "POST",
      url: "/destinations",
      data: formData,
      success: function(destinationHtml){
	$("#show-destinations").append(destinationHtml);	
      }
    });
  });
############
  $("body").on("click", ".item-submit", function(event){
    event.preventDefault();
    var formData = $(event.currentTarget).closest("form").serialize();
    $.ajax({
      type: "POST",
      url: "/destinations",
      data: formData,
      success: function(destinationHtml){
	$(".show-todo-items").append(destinationHtml);	
      }
    });
  });
  */
});

